// import * as Stripe from "stripe"
/*import { PaymentVerificationI, StripeArgs } from './payment';

type Order = {
  readonly stripeArgs: StripeArgs;
};

export const purchaseCoffee =
  (verifier: PaymentVerificationI) =>
  (order: Order): boolean => {
    if (verifier.verifyPayment(order.stripeArgs)) {
      return true;
    }
    return false;
  };*/
